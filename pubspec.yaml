name: render_protocol_server
description: A server app using the shelf package and Docker.
version: 0.0.1
repository: https://github.com/renderprotocol/render_protocol_server

environment:
  sdk: ^3.10.1

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
  shelf_web_socket: ^3.0.0
  shelf_essentials: ^1.0.1
  meta: ^1.17.0

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
